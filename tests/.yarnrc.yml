# Cypress ships a platform binary and resolves plugins from disk, so it needs a real
# node_modules tree — it does not work under Yarn's default PnP linker.
nodeLinker: node-modules
enableScripts: true
